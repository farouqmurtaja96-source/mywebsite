import { LESSON_ID_HEALTH } from '../../core/constants.js';
import { makePlaceholderDialogue } from '../_placeholder.js';

export const dialogueId = 'BD::' + LESSON_ID_HEALTH;
export const dialogue = makePlaceholderDialogue({
  dialogueId,
  level: 'Pre-Intermediate',
  unit: 'Health & Emergencies',
  title: 'Health & Emergencies - Decisions',
});
